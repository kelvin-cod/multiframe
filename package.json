{
  "name": "multiframe-workspace",
  "version": "1.0.0",
  "type": "module",
  "description": "Premium multi-frame dashboard with credentials vault",
  "scripts": {
    "dev": "vite --port 3000 --open",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "vite": "^6.0.0",
    "wrangler": "^4.0.0"
  }
}
